import 'package:flutter/material.dart';

class LibraryPlaylistCard extends StatelessWidget {
  const LibraryPlaylistCard({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
